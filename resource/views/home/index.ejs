
    <!-- Jumbotron Header -->
<header class="jumbotron hero-spacer text-dark ">
  <h3>به دنیای فیتنس خوش آمدید!</h3>
<p>اپلیکیشن فیتکس به شما کمک میکنه تا حرکات ورزشیِ بدون نباز به تجهیزات رو به سادگی یاد بگیرین ،
    به شما برنامه تمرینی میده و کمکتون میکنه تو این راه تنها نباشین و پیشرفت کنین
    و البته یه بخش ویژه بعنوان مقالات داره و راجع به سلامت و تغذیه و ورزش بطور تخصصی توش نوشته میشه و میتونین ازش استفاده کنین...</>
</header>



    <div class="row mb-3">
        <div class="d-inline p-2 bg-dark text-white  m-3">
            <h3 > خواندنی های فیتکس</h3>
        </div>
    </div>
    <!-- header big articles-->

    <div class="row">
        <% headers.forEach(head => { %>
        <div class="col-12 col-md-6">
        <img class="card-img-top headHeight" src="<%= head.thumb %>" alt="<%= head.title%>">
        <div class="card-body">
            <h2 class="card-title"><%= head.title%></h2>
            <p class="card-text"><%- head.body.substr(0,220)%></p>
            <a href="<%= head.path() %>" class="btn btn-primary">ادامه مطلب →</a>

        </div>
        <div class="card-footer text-muted">
            <%= date(head.createdAt).format('jD jMMMM jYYYY') %>
            <a href="https://fitexapp.ir/">فیتکس</a>
        </div>
        </div>
       <% }) %>

    </div>
<div class="row">
    <% courses.forEach(course => { %>
      <div class="col-6 col-md-3">
          <div class="card ">
                  <img class="card-img-top cardHeight" src="<%= course.thumb %>" alt="<%= course.title %>">
              <div class="card-body ">
                <h3><a href="<%= course.path() %>"><%= course.title %></a></h3>
                <p class="card-text"><%- course.body.substr(0,80) %>...</p>
                  <div class=" ">
                      <a href="<%= course.path() %>" class="btn btn-info btn-sm cen">اطلاعات بیشتر</a>

                  </div>
              </div>
              <div class="card-footer text-muted">
                  <span>ارسال شده در <%= date(course.createdAt).format('jD jMMMM jYYYY') %></span>
                <!--<%= course.viewCount %> بازدید-->
              </div>
          </div>
      </div>
    <% }) %>
</div>
